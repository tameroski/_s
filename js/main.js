(function($) {

	// all Javascript code goes here

    $(document).ready(function (){

        // Modernizr will tell if touch events are available or not
        clickEvent = Modernizr.touch ? 'touchstart' : 'click';
        
    });

})(jQuery);
